export async function ensureForumSchema(pool){const[cols]=await pool.query("SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_SCHEMA=DATABASE() AND TABLE_NAME='forum_topics'");const set=new Set(cols.map(x=>x.COLUMN_NAME));if(!set.has('category'))await pool.query("ALTER TABLE forum_topics ADD COLUMN category VARCHAR(40) NOT NULL DEFAULT 'general'");if(!set.has('document_id'))await pool.query("ALTER TABLE forum_topics ADD COLUMN document_id INT NULL");await pool.query("CREATE TABLE IF NOT EXISTS forum_reactions(topic_id INT NOT NULL,user_id INT NOT NULL,created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,PRIMARY KEY(topic_id,user_id)) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4")}
