import{getPool}from"@/lib/db";import{getCurrentUser}from"@/lib/auth";import{ensureContractSchema}from"@/lib/contracts";export async function PUT(req,{params}){const me=await getCurrentUser();if(!me?.isAdmin)return Response.json({error:"No autorizado"},{status:401});const b=await req.json(),p=getPool();await ensureContractSchema(p);await p.query("UPDATE contract_document_requests SET name=?,description=?,section=?,required=?,due_date=?,applies_to=?,assigned_user_id=?,allow_multiple=? WHERE id=? AND contract_id=?",[b.name,b.description,b.section,b.required?1:0,b.due_date||null,b.applies_to,b.assigned_user_id||null,b.allow_multiple?1:0,params.requestId,params.id]);return Response.json({ok:true})}export async function DELETE(_r,{params}){const me=await getCurrentUser();if(!me?.isAdmin)return Response.json({error:"No autorizado"},{status:401});const p=getPool();await ensureContractSchema(p);await p.query("DELETE FROM contract_document_requests WHERE id=? AND contract_id=?",[params.requestId,params.id]);return Response.json({ok:true})}
