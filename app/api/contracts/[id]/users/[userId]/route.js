import{getPool}from"@/lib/db";import{getCurrentUser}from"@/lib/auth";import{ensureContractSchema,addContractEvent}from"@/lib/contracts";export async function DELETE(_r,{params}){const me=await getCurrentUser();if(!me?.isAdmin)return Response.json({error:"No autorizado"},{status:401});const p=getPool(),id=Number(params.id),uid=Number(params.userId);await ensureContractSchema(p);await p.query("DELETE FROM contract_users WHERE contract_id=? AND user_id=?",[id,uid]);await p.query("DELETE FROM contract_members WHERE contract_id=? AND user_id=?",[id,uid]);await addContractEvent(p,id,me.id,'user_removed','Usuario retirado del contrato');return Response.json({ok:true})}
