import{getPool}from"@/lib/db";import{getCurrentUser}from"@/lib/auth";import{ensureContractSchema,canAccessContract}from"@/lib/contracts";export async function GET(_r,{params}){const me=await getCurrentUser(),p=getPool(),id=Number(params.id);await ensureContractSchema(p);if(!me||!await canAccessContract(p,me,id))return Response.json({error:"No autorizado"},{status:403});const[x]=await p.query("SELECT e.*,u.full_name actor_name,DATE_FORMAT(e.created_at,'%Y-%m-%d %H:%i') created_at FROM contract_events e LEFT JOIN users u ON u.id=e.actor_user_id WHERE e.contract_id=? ORDER BY e.created_at DESC",[id]);return Response.json(x)}
