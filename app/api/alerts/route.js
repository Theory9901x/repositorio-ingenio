import{getCurrentUser}from"@/lib/auth";import{getPool}from"@/lib/db";import{ensurePlatformSchema}from"@/lib/platformSchema";export const dynamic="force-dynamic";export async function GET(){const me=await getCurrentUser();if(!me)return Response.json({error:"No autorizado"},{status:401});const p=getPool();await ensurePlatformSchema(p);if(me.isAdmin){await p.query(`INSERT INTO system_alerts(contract_id,related_type,related_id,title,message,severity) SELECT c.id,'contract',c.id,CONCAT('Contrato próximo a vencer: ',c.title),CONCAT('Finaliza el ',DATE_FORMAT(c.end_date,'%Y-%m-%d')),IF(c.end_date<CURDATE(),'danger','warning') FROM contract_routes c WHERE c.end_date<=DATE_ADD(CURDATE(),INTERVAL 30 DAY) AND c.status NOT IN('finalizado') AND NOT EXISTS(SELECT 1 FROM system_alerts a WHERE a.related_type='contract' AND a.related_id=c.id AND a.status<>'resolved')`)}await p.query(`INSERT INTO system_alerts(user_id,contract_id,related_type,related_id,title,message,severity) SELECT a.user_id,a.contract_id,'activity',a.id,CONCAT('Actividad requiere atención: ',a.title),COALESCE(a.admin_comment,'Revisa el estado de la actividad'),'danger' FROM contract_activities a WHERE a.status IN('rejected','needs_changes') AND NOT EXISTS(SELECT 1 FROM system_alerts x WHERE x.related_type='activity' AND x.related_id=a.id AND x.status<>'resolved')`);const[rows]=await p.query(`SELECT a.*,c.title contract_name,DATE_FORMAT(a.created_at,'%Y-%m-%d %H:%i') created_at FROM system_alerts a LEFT JOIN contract_routes c ON c.id=a.contract_id WHERE (? OR a.user_id=? OR(a.user_id IS NULL AND EXISTS(SELECT 1 FROM contract_members m WHERE m.contract_id=a.contract_id AND m.user_id=?))) AND a.status<>'resolved' ORDER BY a.status='unread' DESC,FIELD(a.severity,'danger','warning','info','success'),a.created_at DESC LIMIT 80`,[me.isAdmin?1:0,me.id,me.id]);return Response.json(rows)}
