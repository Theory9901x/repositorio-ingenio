import { getCurrentUser } from "@/lib/auth";import { getPool } from "@/lib/db";import { ensureFeatureSchema } from "@/lib/featureSchema";
export const dynamic="force-dynamic";
export async function GET(){const me=await getCurrentUser();if(!me)return Response.json({error:"No autorizado"},{status:401});const p=getPool();await ensureFeatureSchema(p);const [topics]=await p.query(`SELECT t.id,t.title,t.body,t.author_id,t.contract_id,t.is_published,DATE_FORMAT(t.created_at,'%Y-%m-%d %H:%i') created_at,u.full_name author_name,u.cargo,COUNT(c.id) comment_count FROM forum_topics t JOIN users u ON u.id=t.author_id LEFT JOIN forum_comments c ON c.topic_id=t.id WHERE t.is_published=1 AND (t.contract_id IS NULL OR ? OR EXISTS(SELECT 1 FROM contract_members m WHERE m.contract_id=t.contract_id AND m.user_id=?)) GROUP BY t.id ORDER BY t.created_at DESC`,[me.isAdmin?1:0,me.id]);return Response.json(topics);}
export async function POST(req){const me=await getCurrentUser();if(!me)return Response.json({error:"No autorizado"},{status:401});const b=await req.json();if(!b.title?.trim()||!b.body?.trim())return Response.json({error:"Título y contenido requeridos"},{status:400});const p=getPool();await ensureFeatureSchema(p);await p.query("INSERT INTO forum_topics(title,body,author_id,contract_id,is_published) VALUES(?,?,?,?,1)",[b.title.trim(),b.body.trim(),me.id,b.contractId||null]);return Response.json({ok:true});}
export async function DELETE(req){const me=await getCurrentUser();if(!me)return Response.json({error:"No autorizado"},{status:401});const id=Number(new URL(req.url).searchParams.get("id"));const p=getPool();await ensureFeatureSchema(p);await p.query("DELETE FROM forum_topics WHERE id=? AND (author_id=? OR ?)",[id,me.id,me.isAdmin?1:0]);return Response.json({ok:true});}
