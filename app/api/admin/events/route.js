import{getCurrentUser}from"@/lib/auth";import{getPool}from"@/lib/db";import{ensureAdminSchema}from"@/lib/adminSchema";export const dynamic="force-dynamic";
export async function GET(){const me=await getCurrentUser();if(!me?.isAdmin)return Response.json({error:"No autorizado"},{status:403});const p=getPool();await ensureAdminSchema(p);const[rows]=await p.query(`SELECT e.*,u.full_name actor_name,DATE_FORMAT(e.created_at,'%Y-%m-%d %H:%i:%s') created_at_formatted FROM admin_events e LEFT JOIN users u ON u.id=e.actor_user_id ORDER BY e.created_at DESC LIMIT 200`);return Response.json(rows)}
